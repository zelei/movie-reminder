<div ng-controller="SearchListWebController">
                         
    <span ng-cloak ng-show="movies.length == 0 && noResult && !searching">No result.</span>
    
    <ul class="unstyled movie-list" ng-cloak>
        <li class="movie" ng-class="{selected: movie.selected}" ng-repeat="movie in movies">
            <div class="media">     
            	<img class="img-polaroid pull-left" ng-src="{{movie.thumbnail}}" style="width: 61px; height: 91px;">
    	        <h4 class="movie-title">
                    <span>{{movie.title}}&nbsp;</span>
                    <% if @user : %>
                        <i ng-hide="movie.saving" ng-click="select(movie)" class="pointer icon-white" ng-class="{'icon-minus': movie.selected, 'icon-plus': !movie.selected}"></i>
                        <i ng-show="movie.saving" class="pointer icon-white icon-ban-circle"></i>   
                    <% end %>                          
                </h4>
                <div><i ng-show="movie.releaseDate">({{movie.releaseDate}})</i></div>           
                <span ng-repeat="link in movie.links">
                    <a href="{{link.url}}" target="_blank">{{link.name}}</a>
                    <span ng-show="!$last">/</span>
                </span>
                <read synopsis="{{movie.synopsis}}" key="{{movie.id}}" key-store="$parent.openedDescription"></read>
            </div>
        </li>    
    </ul>

    <hr ng-cloak ng-show="movies.length != 0" >
    
</div> 